---
date: 2026-07-10
type: status-note
purpose: explain grounding for today's content and flag an operational risk found while verifying
---

# Status — 2026-07-10 content cycle

## What I checked

- `git log` (lab repo): last commit is `f6d9952` (2026-06-30) on main history; branch
  `fix/consent-registry-assert-parens` is 1 commit ahead of its origin with
  `0509b28` (the 2026-07-09 content commit). No source-code commits since 06-30.
- `git status` (lab repo): same two modified files as the last four days —
  `scripts/midnight-health-check.sh` (mtime 2026-07-07) and
  `scripts/pre-deploy-check.sh` (mtime 2026-07-05). Nothing newer.
- `find . -newer content/2026-07-09/twitter-thread-still-uncommitted.md`: only
  build artifacts under `build/` (compiled `.zkir`/prover/verifier files —
  regenerated by `compactc`, not source edits) and yesterday's own content/zealy
  files. No source file changed since 2026-07-09.
- DNA repo: only uncommitted change is `quests/night-force-quests.yaml`,
  unrelated to today, unchanged since 2026-06-18.
- No new dev log exists past `logs/2026-06-30-dev.md`.

## What's new today (not a repeat of 07-09)

Checked `/var/log/midnight-health/health.log`, the real cron output of
`midnight-health-check.sh` — this had not been inspected in prior cycles. Finding:

- The uncommitted `check_proof_server()` fix (in the file since 2026-07-07) is
  **actively executing in production**, every 2 hours, via cron. Cron reads the
  file on disk, not `git status` — so "uncommitted" and "not deployed" are not
  the same thing here.
- Today alone (02:00–14:00 UTC), it ran 9 times and correctly flagged the same
  known squatter container (`proof-server` version `8.0.3` on port 6300,
  expected `7.0.0`) all 9 times.
- The standalone `midnight-node`/`midnight-indexer` containers are also failing
  every check (`FAIL: node RPC nao responde`, `FAIL: indexer GraphQL nao
  responde`) — but this is chronic, present in the log continuously since
  2026-05-01, already disclosed in the 2026-07-07 thread ("meu stack
  midnight-standalone-* nem existe no host agora"). Not new, not today's angle.

This is genuine new information (verified against the actual production log,
not inferred), so today's content covers a distinct angle — version-control
gap on a live decision-making script — rather than re-running the "still
uncommitted" narrative from 2026-07-09 verbatim.

## Conclusion

Still no real dev/source work between 2026-07-09 and 2026-07-10 — this is
day 2 in a row. Rather than invent progress or repeat yesterday's exact
framing, today's pieces are grounded in a fact discovered while verifying
that state: the pending fix is not just written, it is live and correct in
production, unversioned. That is itself worth flagging operationally.

## What's actually missing / blocking

1. The version-check fix (proof-server `/version` compare, node/indexer tag
   compare) is still **not committed**, in either `scripts/midnight-health-check.sh`
   (uncommitted 3 days) or `scripts/pre-deploy-check.sh` (uncommitted 5 days) —
   despite being proven correct in production 9/9 times today.
2. The digest-vs-tag gap flagged on 2026-07-08 is still open.
3. The standalone node/indexer stack has been down since 2026-05-01 (over two
   months) — chronic, already known, unrelated to the version-check diff.
4. From the 2026-06-30 log's "Next Steps" (still unstarted): first standalone
   deploy via `deploy-all.ts`, verification via `status.ts`, full lifecycle via
   `interact-full-suite.ts`.

## Recommendation

Commit the pending version-check diff now — it has been running correctly in
production for 3-5 days, which is stronger evidence of correctness than most
commits get before merge. Leaving a production incident-decision script
outside version control is a real (if small) operational risk, independent of
whether new feature work resumes.
