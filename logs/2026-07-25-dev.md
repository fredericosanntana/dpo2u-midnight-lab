# Dev Log — 2026-07-25

**Agent**: DPO2U Midnight Agent (Claude Sonnet 5)
**Session type**: Development Cycle — finish and commit pending fix
**Branch**: fix/consent-registry-assert-parens

---

## Pre-flight checks

- DNA files read: `SDK-VERSION-MATRIX.md` (preprod table: indexer-standalone **3.1.0**, node
  0.21.0, proof-server 7.0.0, compactc 0.29.0 — repo's compactc has since moved to 0.31.0, already
  a known/accepted drift from prior sessions), `WORKAROUND-GUIDE.md` (7 bugs, all still applicable).
- `git status`: two files modified but uncommitted — `docker-compose.yml` (indexer image tag
  `4.0.0-rc.4` → `3.1.0`) and `scripts/pre-deploy-check.sh` (`INDEXER_VERSION` constant, same
  change, with a comment dated 2026-07-24). This is the in-progress work referenced by step 1 of
  the task.
- Also present in the working tree: a large batch of already-staged `content/` and `zealy/`
  files (dates 2026-07-03 through 2026-07-23) plus untracked `HUMAN-ACTIONS.md`,
  `PR-BODY-technical.md`, `ZEALY-LEVANTAMENTO.md`, `zealy-submit/`. Per
  `zealy/2026-07-23/status-note-2026-07-23.md`, the content/zealy batch and the separate
  Zealy-submission workstream are owned by other pipelines/cycles, not this dev cycle. Left
  untouched — this session commits only the dev-scoped files below, same scoping `da60821`
  (2026-07-16) used.

This is exactly the pattern from the 2026-07-15/07-16 sessions: a version-drift fix was written
and staged in the working tree but never committed. This time the drift is the opposite
direction — the compose file and script had drifted to a PREVIEW-tier indexer tag
(`4.0.0-rc.4`), which `SDK-VERSION-MATRIX.md`'s preprod table says does not pair with a
0.21.0 node; the pending diff corrects both back to `3.1.0`.

---

## Verification performed

1. **`compactc --version`** (with `~/.compact/bin` on PATH) — `0.31.0`, matches the
   `COMPACT_VERSION` constant already in `pre-deploy-check.sh` and in `compile-contracts.sh`.
   Unrelated to this session's diff; confirmed no regression.
2. **Full contract compile** — `./scripts/compile-contracts.sh`:
   ```
   ConsentRegistry   — 8 circuits  — OK
   DataAuditLog      — 11 circuits — OK
   DataSubjectRights — 12 circuits — OK
   Results: 3 compiled, 0 failed
   ```
   No contract source touched. Confirms no fix needed under task step (a), and no new contract
   needed under (b) — exactly 3 present. Falls to (c): finish the already-drafted deploy-script
   fix.
3. **`docker-compose.yml` cross-check** — line 24 now reads
   `image: midnightntwrk/indexer-standalone:3.1.0`, matching `SDK-VERSION-MATRIX.md`'s preprod
   table exactly (previously `4.0.0-rc.4`, the table's own PREVIEW-tier value — a real mismatch,
   not a stale-matrix false positive like the one flagged in the 2026-07-16 log).
4. **Ran `./scripts/pre-deploy-check.sh --network standalone`** live on this host:
   ```
   [OK]   Node.js 22.22.0
   [OK]   compactc 0.31.0
   [OK]   No .npmrc
   [OK]   ConsentRegistry / DataAuditLog / DataSubjectRights — build artifacts present
   [FAIL] midnight-node not responding on :9944
   [FAIL] indexer-standalone not responding on :8088
   [FAIL] proof-server on :6300 is version '8.0.3', expected 7.0.0 (squatter, same one
          documented since content/2026-07-07 — dpo2u-midnight-self-funding-proof-server-1)
   [FAIL] midnight-node container 'midnight-standalone-node' not running
   [FAIL] indexer-standalone container 'midnight-standalone-indexer' not running
   RESULTS: 7 passed, 5 failed
   ```
   `docker ps -a` confirms: no `midnight-standalone-node` / `midnight-standalone-indexer`
   containers exist on this host at all (standalone stack still down, chronic since 2026-05-01 —
   unchanged from every prior session). All 5 failures are pre-existing infra-down conditions,
   not caused by or exposed by this session's diff. No way to positively confirm the indexer
   fix against a live 3.1.0 container on this host — that check can only be exercised once the
   standalone stack is brought up, which is still open (see below).

Conclusion: the pending diff is internally consistent with `SDK-VERSION-MATRIX.md` and does not
regress compilation or the check script's other paths. Committing as-is, scoped to
`docker-compose.yml` + `scripts/pre-deploy-check.sh` only.

---

## Files committed

- `docker-compose.yml` — indexer image `midnightntwrk/indexer-standalone:4.0.0-rc.4` →
  `midnightntwrk/indexer-standalone:3.1.0`, matching `SDK-VERSION-MATRIX.md` preprod table.
- `scripts/pre-deploy-check.sh` — `INDEXER_VERSION` constant updated to match, with a comment
  recording the fix and the reason (PREVIEW-tier tag mismatched against preprod node 0.21.0).

---

## What's still open (carried from prior logs, unrelated to this session)

1. First standalone deploy — `npx tsx scripts/deploy-all.ts --network standalone` — still not run.
2. Standalone `midnight-node` / `midnight-indexer` containers still absent on this host (down
   since 2026-05-01) — the indexer version fix in this commit cannot be exercised end-to-end
   until the stack is brought up.
3. `scripts/image-digests.lock` has no real entries — run `./scripts/pin-image-digest.sh --all`
   once the standalone stack is up and verified.
4. The `:6300` proof-server squatter (`dpo2u-midnight-self-funding-proof-server-1`, version
   8.0.3) is still running and still the wrong version for this repo's checks — unrelated
   project, out of scope to stop from this repo.
