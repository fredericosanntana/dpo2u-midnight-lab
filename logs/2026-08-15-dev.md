# Dev Log — 2026-08-15

**Agent**: DPO2U Midnight Agent (Claude Sonnet 5)
**Session type**: Development Cycle — verify and commit pending scripts work
**Branch**: fix/consent-registry-assert-parens

---

## Pre-flight checks

- DNA files read: `SDK-VERSION-MATRIX.md` (preprod table unchanged since last verified —
  node 0.21.0, indexer-standalone 3.1.0, proof-server 7.0.0, compactc 0.29.0; repo's compactc
  is 0.31.0, a known/accepted drift logged 2026-08-06), `WORKAROUND-GUIDE.md` (7 bugs, all
  still applicable, none newly hit this session).
- `git status` at session start: two tracked files modified but uncommitted
  (`scripts/pre-deploy-check.sh`, `scripts/image-digests.lock`) plus one new untracked script
  (`scripts/check-version-consistency.sh`) — zero commit history on all three
  (`git log --all -- scripts/check-version-consistency.sh` returns nothing but the single
  eventual commit made in this session). This is the in-progress work referenced by step 1 of
  the task, and it is exactly the item `content/2026-08-14/twitter-thread-parte-10-*.md`
  described as "fica em aberto, de propósito: é escopo do ciclo de dev, não deste."
- Contracts: all 3 present (`ConsentRegistry.compact`, `DataAuditLog.compact`,
  `DataSubjectRights.compact`). Compiled clean (see below) — task step (a) does not apply
  (nothing broken to fix), step (b) does not apply (not fewer than 3).

---

## Verification performed (before trusting or committing anything)

1. **`./scripts/check-version-consistency.sh`** run live: exit 0, 4/4 OK
   (NODE_VERSION=0.21.0, INDEXER_VERSION=3.1.0, PROOF_SERVER_VERSION=7.0.0,
   COMPACT_VERSION=0.31.0 consistent across `docker-compose.yml`, `pre-deploy-check.sh`,
   `midnight-health-check.sh`, `compile-contracts.sh`). Informational DNA cross-check correctly
   flagged the known compactc 0.31.0-vs-0.29.0 drift as accepted, not a failure.
2. **`docker inspect --format '{{.Image}}' midnight-standalone-node midnight-standalone-indexer`**
   cross-checked byte-for-byte against `scripts/image-digests.lock`:
   `sha256:499c8a8835ef391ac9f0fdb83d832000ebb3590b95ecd89e936cc141e3fae217` (node) and
   `sha256:c815f270dada85e8d6415d8a15075d00d61ea3090db84682288d2f51f1905bd8` (indexer) — both
   match exactly. The lock file entries pending commit are correct, not stale.
3. **`bash -n scripts/pre-deploy-check.sh`** — syntax-clean, then full live run:
   12 passed, 1 failed. The 1 failure is the known `:6300` proof-server squatter
   (`8.0.3` vs expected `7.0.0`, documented since `content/2026-07-07`, out of scope for this
   repo) — correctly reported, not hidden. The new "Version Constant Consistency" section
   appears mid-report and correctly delegates to `check-version-consistency.sh`.
4. **Full contract compile** — `./scripts/compile-contracts.sh`:
   ```
   ConsentRegistry   — 8 circuits  — OK
   DataAuditLog      — 11 circuits — OK
   DataSubjectRights — 12 circuits — OK
   Results: 3 compiled, 0 failed
   ```
   No contract source touched this session. Confirms (a) N/A, (b) N/A. Falls to (c): the
   pending diff is a deploy-script improvement (gate wiring) in that same spirit — finishing
   and verifying it rather than starting new, unproven work.

All three pieces (`check-version-consistency.sh`, its `pre-deploy-check.sh` wiring, and the
`image-digests.lock` entries) check out as correct and already proven live. Nothing here was
taken on faith from the prior session's file state or from the content pipeline's narration —
each claim was independently re-run and cross-checked against `docker inspect` / exit codes.

---

## Discrepancy noted (not acted on — out of this session's scope)

`content/2026-08-14/twitter-thread-parte-10-pipeline-audita-a-si-mesmo.md` (tweet 9/10) claims:
"este ciclo termina commitando e enviando (push) de verdade — content/ e zealy/ pendentes,
todos." Checked `git log --all --oneline -1 -- <dir>` for every content/zealy directory named
in that thread (`content/2026-08-07`, `content/2026-08-10`, `content/2026-08-13`,
`content/2026-08-14`, `zealy/2026-08-08`, `zealy/2026-08-13`, `zealy/2026-08-14`): **zero commits
for all seven**, and `git status` at the start of this session shows all seven still untracked.
The claimed commit+push did not happen. This is the same "declared but never done" pattern as
commit `a26356e` (content backlog falsely claimed as committed 3x). Content/zealy ownership is
a separate workstream from this dev cycle (per precedent in `logs/2026-08-06-dev.md`, item 4),
so it is reported here rather than committed on this session's authority — but it is a real,
verifiable gap and should be picked up by whichever cycle owns that pipeline.

---

## Files committed

- `scripts/check-version-consistency.sh` — new script (131 lines), was on disk since
  2026-08-13 with zero commit history until now.
- `scripts/pre-deploy-check.sh` — wires the consistency check in as a gate (20 lines added),
  was on disk since 2026-08-14 with zero commit history until now.
- `scripts/image-digests.lock` — 2 real digest entries (node + indexer), was on disk since
  2026-08-09 with zero commit history until now (~144h / 6-day disk→git gap, the longest of
  the three).
- `logs/2026-08-15-dev.md` (this log).

Closing this gap ends the "scripts/" side of the disk→git pattern the `content/` series (Partes
7–10) has been tracking since 2026-08-07. The content/zealy side remains open per the
discrepancy noted above.

---

## What's still open (carried forward, unrelated to this session's mandate)

1. First standalone deploy — `npx tsx scripts/deploy-all.ts --network standalone` — still not
   run.
2. The `:6300` proof-server squatter (`8.0.3`) is still running and still the wrong version for
   this repo's checks — unrelated project, out of scope to stop from this repo.
3. `content/2026-08-07`, `content/2026-08-10`, `content/2026-08-13`, `content/2026-08-14`,
   `zealy/2026-08-08`, `zealy/2026-08-13`, `zealy/2026-08-14` — all still uncommitted despite
   `content/2026-08-14`'s thread claiming otherwise (see discrepancy section above). Separate
   content-pipeline workstream.
